========================
BUILD OUTPUT DESCRIPTION
========================
The program needs JRE (Java Runtime Environment) and NetBeans in your Operating System
To run program, you should import the whole project folder (DocsOn) in NetBeans by clicking open project and selecting the project folder in NetBeans.
After that, execute project and start to use the program with login the window.
To connect the database, you should set the MySQL server or SQLite local file in the login page by clicking the SQL option button in the application.
Notes:
* If program gives you ClassNotFoundException, you should remove the libraries from project and re-add them into the project.
* When you open the project in NetBeans if the project gives an error with the libraries, you should remove the libraries from project and re-add them into the project.
* If program gives you CommunicationsException error, you should check your Internet connection.

========================
LOGIN DOCS-ON SYSTEM
========================

Docs-On comes with the default MySQL Server in its source code.
MySQL Server : sql4.freesqldatabase.com:3306/sql4100665
MySQL Username: sql4100665
MySQL Password : VUPqKzEaEq
*If you use our database, no need to change these options.

--------------------------------------------------

To login our system, you can use the following accounts
/Head Doctor account/ Username: admin, Password: admin
/Doctor account/ Username: pinar, Password: pinar123

--------------------------------------------------
To search a patient, you can use the following ID number
21301890
